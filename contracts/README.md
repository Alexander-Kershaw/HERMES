# Constracts Note

The data contracts here are also copied into `src/hermes/contracts` so that the contracts may be packaged directly with the rest of the HERMES modules for use in Azure Databricks.

I did this to ensure the YAML contracts are avaliable when HERMES is installed on Databricks or some other runtime environment.

